/* Timavelle admin links: keep every escape route pointed at the public cuisine website. */
export const PUBLIC_SITE_URL = import.meta.env.VITE_PUBLIC_SITE_URL || 'https://timavelle-cuisine.vercel.app';
