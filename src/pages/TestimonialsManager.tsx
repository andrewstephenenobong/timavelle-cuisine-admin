import { useCallback, useEffect, useState } from 'react';
import api from '../lib/api';
import ConfirmDialog from '../components/ConfirmDialog';

interface Testimonial { _id: string; clientName: string; quote: string; eventType?: string; featured: boolean; archivedAt?: string; }
type Scope = 'active' | 'archived'; type ArchiveAction = 'archive' | 'restore' | 'delete'; type PendingAction = { testimonial: Testimonial; action: ArchiveAction };
const emptyForm = { clientName: '', quote: '', eventType: '', featured: false }; type TestimonialFormErrors = Partial<Record<'clientName' | 'quote', string>>;
function dialogCopy(action: ArchiveAction, name: string) { return action === 'archive' ? { title: `Archive ${name}?`, message: 'This testimonial will be hidden from public content and kept in the archive until restored.', label: 'Archive', busy: 'Archiving…' } : action === 'restore' ? { title: `Restore ${name}?`, message: 'This testimonial will return to active public content.', label: 'Restore', busy: 'Restoring…' } : { title: `Permanently delete ${name}?`, message: 'This archived testimonial will be permanently deleted and cannot be restored.', label: 'Delete permanently', busy: 'Deleting…' }; }

export default function TestimonialsManager() {
  const [scope, setScope] = useState<Scope>('active'); const [testimonials, setTestimonials] = useState<Testimonial[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [form, setForm] = useState(emptyForm); const [editingId, setEditingId] = useState<string | null>(null); const [showForm, setShowForm] = useState(false); const [saving, setSaving] = useState(false); const [loadFailed, setLoadFailed] = useState(false); const [formErrors, setFormErrors] = useState<TestimonialFormErrors>({}); const [pendingAction, setPendingAction] = useState<PendingAction | null>(null); const [actionId, setActionId] = useState<string | null>(null);
  const fetchTestimonials = useCallback(async () => { setLoading(true); setLoadFailed(false); try { const res = await api.get('/api/testimonials/admin', { params: { scope } }); setTestimonials(res.data.testimonials); } catch { setError('Could not load testimonials.'); setLoadFailed(true); } finally { setLoading(false); } }, [scope]);
  useEffect(() => { const timer = window.setTimeout(() => { void fetchTestimonials(); }, 0); return () => window.clearTimeout(timer); }, [fetchTestimonials]);
  function openCreateForm() { setForm(emptyForm); setFormErrors({}); setEditingId(null); setShowForm(true); }
  function openEditForm(t: Testimonial) { setForm({ clientName: t.clientName, quote: t.quote, eventType: t.eventType || '', featured: t.featured }); setFormErrors({}); setEditingId(t._id); setShowForm(true); }
  async function handleSubmit(e: React.FormEvent) { e.preventDefault(); const nextErrors: TestimonialFormErrors = {}; if (!form.clientName.trim()) nextErrors.clientName = 'Add the client name for this testimonial.'; if (!form.quote.trim()) nextErrors.quote = 'Add the approved testimonial copy before saving.'; if (Object.keys(nextErrors).length) { setFormErrors(nextErrors); return; } setSaving(true); setError(''); setFormErrors({}); try { if (editingId) await api.put(`/api/testimonials/${editingId}`, form); else await api.post('/api/testimonials', form); setShowForm(false); await fetchTestimonials(); } catch { setError('Something went wrong saving this testimonial.'); } finally { setSaving(false); } }
  async function confirmAction() { if (!pendingAction) return; const { testimonial, action } = pendingAction; setActionId(testimonial._id); setError(''); try { if (action === 'delete') await api.delete(`/api/testimonials/${testimonial._id}`); else await api.post(`/api/testimonials/${testimonial._id}/${action}`); setPendingAction(null); await fetchTestimonials(); } catch { setError(`Something went wrong ${action === 'delete' ? 'permanently deleting' : `${action}ing`} this testimonial.`); } finally { setActionId(null); } }
  const dialog = pendingAction ? dialogCopy(pendingAction.action, pendingAction.testimonial.clientName) : null;
  return <div><div className="mb-6 flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-display text-2xl font-semibold text-emerald-deep">Testimonials</h2><p className="mt-1 font-body text-sm text-stone">Archive testimonials first to preserve a recoverable catalog.</p></div>{scope === 'active' && <button onClick={openCreateForm} className="rounded-full bg-gold px-5 py-2 font-utility text-sm font-medium text-emerald-deep hover:bg-emerald hover:text-ivory">+ Add Testimonial</button>}</div><div className="mb-5 flex gap-2" aria-label="Testimonial catalog view"><button onClick={() => { setScope('active'); setShowForm(false); }} className={`rounded-full px-4 py-2 font-utility text-xs ${scope === 'active' ? 'bg-emerald text-ivory' : 'border border-stone/30 text-stone'}`}>Active testimonials</button><button onClick={() => { setScope('archived'); setShowForm(false); }} className={`rounded-full px-4 py-2 font-utility text-xs ${scope === 'archived' ? 'bg-emerald text-ivory' : 'border border-stone/30 text-stone'}`}>Archive</button></div>{error && <div className="mb-4 flex flex-wrap items-center justify-between gap-3 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700" role="alert"><span>{error}</span>{loadFailed && <button type="button" onClick={() => void fetchTestimonials()} className="font-utility text-xs underline">Retry loading</button>}</div>}{showForm && <form onSubmit={handleSubmit} noValidate className="mb-8 rounded-3xl bg-white p-6 shadow-lg"><h3 className="mb-4 font-display text-lg font-semibold text-ink">{editingId ? 'Edit Testimonial' : 'New Testimonial'}</h3><label htmlFor="testimonial-client" className="sr-only">Client name</label><input id="testimonial-client" value={form.clientName} onChange={(e) => setForm({ ...form, clientName: e.target.value })} placeholder="Client name" aria-invalid={Boolean(formErrors.clientName)} aria-describedby={formErrors.clientName ? 'testimonial-client-error' : undefined} className="w-full rounded-xl border border-stone/20 px-4 py-2 font-body" />{formErrors.clientName && <p id="testimonial-client-error" className="mt-1 text-xs text-red-600">{formErrors.clientName}</p>}<label htmlFor="testimonial-quote" className="sr-only">Approved testimonial quote</label><textarea id="testimonial-quote" value={form.quote} onChange={(e) => setForm({ ...form, quote: e.target.value })} placeholder="Quote" aria-invalid={Boolean(formErrors.quote)} aria-describedby={formErrors.quote ? 'testimonial-quote-error' : undefined} rows={3} className="mt-4 w-full rounded-xl border border-stone/20 px-4 py-2 font-body" />{formErrors.quote && <p id="testimonial-quote-error" className="mt-1 text-xs text-red-600">{formErrors.quote}</p>}<label htmlFor="testimonial-event" className="sr-only">Event type</label><input id="testimonial-event" value={form.eventType} onChange={(e) => setForm({ ...form, eventType: e.target.value })} placeholder="Event type (optional, e.g. Private dinner)" className="mt-4 w-full rounded-xl border border-stone/20 px-4 py-2 font-body" /><label className="mt-4 flex items-center gap-2 font-body text-sm text-stone"><input id="testimonial-featured" type="checkbox" checked={form.featured} onChange={(e) => setForm({ ...form, featured: e.target.checked })} />Featured on homepage</label><div className="mt-6 flex gap-3"><button type="submit" disabled={saving} className="rounded-full bg-emerald px-6 py-2 font-utility text-sm text-ivory hover:bg-emerald-deep disabled:opacity-50">{saving ? 'Saving…' : 'Save'}</button><button type="button" onClick={() => setShowForm(false)} className="rounded-full border border-stone/30 px-6 py-2 font-utility text-sm text-stone hover:bg-stone/10">Cancel</button></div></form>}{loading ? <p className="font-body text-stone">Loading…</p> : testimonials.length === 0 ? <p className="font-body text-stone">{scope === 'archived' ? 'No archived testimonials.' : 'No testimonials yet. Add your first one above.'}</p> : <div className="grid gap-4">{testimonials.map((t) => <div key={t._id} className="flex items-start justify-between gap-4 rounded-2xl bg-white p-5 shadow"><div><div className="flex items-center gap-2"><h3 className="font-display text-lg font-semibold text-ink">{t.clientName}</h3>{t.featured && <span className="rounded-full bg-gold/20 px-2 py-0.5 font-utility text-xs text-emerald-deep">Featured</span>}</div>{t.eventType && <p className="font-utility text-xs uppercase tracking-wide text-stone">{t.eventType}</p>}<p className="mt-1 font-body text-sm italic text-stone">&ldquo;{t.quote}&rdquo;</p>{t.archivedAt && <p className="mt-2 font-utility text-xs text-stone">Archived {new Date(t.archivedAt).toLocaleString()}</p>}</div><div className="flex shrink-0 flex-wrap gap-2">{scope === 'active' ? <><button onClick={() => openEditForm(t)} className="rounded-full border border-emerald px-4 py-1.5 font-utility text-xs text-emerald hover:bg-emerald hover:text-ivory">Edit</button><button onClick={() => setPendingAction({ testimonial: t, action: 'archive' })} disabled={actionId === t._id} className="rounded-full border border-stone/40 px-4 py-1.5 font-utility text-xs text-stone hover:bg-stone/10">Archive</button></> : <><button onClick={() => setPendingAction({ testimonial: t, action: 'restore' })} disabled={actionId === t._id} className="rounded-full border border-emerald px-4 py-1.5 font-utility text-xs text-emerald hover:bg-emerald hover:text-ivory">Restore</button><button onClick={() => setPendingAction({ testimonial: t, action: 'delete' })} disabled={actionId === t._id} className="rounded-full border border-red-400 px-4 py-1.5 font-utility text-xs text-red-500 hover:bg-red-700 hover:text-white">Delete</button></>}</div></div>)}</div>}<ConfirmDialog open={Boolean(pendingAction)} title={dialog?.title || ''} message={dialog?.message || ''} confirmLabel={dialog?.label} busy={Boolean(actionId)} busyLabel={dialog?.busy} onCancel={() => setPendingAction(null)} onConfirm={() => void confirmAction()} /></div>;
}
