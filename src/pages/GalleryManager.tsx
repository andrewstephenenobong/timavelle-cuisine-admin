import { useCallback, useEffect, useState } from 'react';
import api from '../lib/api';
import ConfirmDialog from '../components/ConfirmDialog';
import ImageUploadField from '../components/ImageUploadField';

interface GalleryImage { _id: string; imageUrl: string; caption?: string; category: string; archivedAt?: string; }
type Scope = 'active' | 'archived'; type ArchiveAction = 'archive' | 'restore' | 'delete'; type PendingAction = { image: GalleryImage; action: ArchiveAction };
const emptyForm = { imageUrl: '', caption: '', category: '' }; type GalleryFormErrors = Partial<Record<'imageUrl' | 'category', string>>;
function dialogCopy(action: ArchiveAction) { return action === 'archive' ? { title: 'Archive this gallery image?', message: 'The image will be hidden from the public gallery and kept in the archive until restored.', label: 'Archive', busy: 'Archiving…' } : action === 'restore' ? { title: 'Restore this gallery image?', message: 'The image will return to the active public gallery.', label: 'Restore', busy: 'Restoring…' } : { title: 'Permanently delete this gallery image?', message: 'This archived image will be permanently deleted and cannot be restored.', label: 'Delete permanently', busy: 'Deleting…' }; }

export default function GalleryManager() {
  const [scope, setScope] = useState<Scope>('active'); const [images, setImages] = useState<GalleryImage[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [form, setForm] = useState(emptyForm); const [editingId, setEditingId] = useState<string | null>(null); const [showForm, setShowForm] = useState(false); const [saving, setSaving] = useState(false); const [loadFailed, setLoadFailed] = useState(false); const [formErrors, setFormErrors] = useState<GalleryFormErrors>({}); const [pendingAction, setPendingAction] = useState<PendingAction | null>(null); const [actionId, setActionId] = useState<string | null>(null);
  const fetchImages = useCallback(async () => { setLoading(true); setLoadFailed(false); try { const res = await api.get('/api/gallery/admin', { params: { scope } }); setImages(res.data.images); } catch { setError('Could not load gallery images.'); setLoadFailed(true); } finally { setLoading(false); } }, [scope]);
  useEffect(() => { const timer = window.setTimeout(() => { void fetchImages(); }, 0); return () => window.clearTimeout(timer); }, [fetchImages]);
  function openCreateForm() { setForm(emptyForm); setFormErrors({}); setEditingId(null); setShowForm(true); }
  function openEditForm(image: GalleryImage) { setForm({ imageUrl: image.imageUrl, caption: image.caption || '', category: image.category }); setFormErrors({}); setEditingId(image._id); setShowForm(true); }
  async function handleSubmit(e: React.FormEvent) { e.preventDefault(); const nextErrors: GalleryFormErrors = {}; if (!form.imageUrl.trim()) nextErrors.imageUrl = 'Upload an image before saving this gallery item.'; if (!form.category.trim()) nextErrors.category = 'Add a category so the gallery can be filtered.'; if (Object.keys(nextErrors).length) { setFormErrors(nextErrors); return; } setSaving(true); setError(''); setFormErrors({}); try { if (editingId) await api.put(`/api/gallery/${editingId}`, form); else await api.post('/api/gallery', form); setShowForm(false); await fetchImages(); } catch { setError('Something went wrong saving this image.'); } finally { setSaving(false); } }
  async function confirmAction() { if (!pendingAction) return; const { image, action } = pendingAction; setActionId(image._id); setError(''); try { if (action === 'delete') await api.delete(`/api/gallery/${image._id}`); else await api.post(`/api/gallery/${image._id}/${action}`); setPendingAction(null); await fetchImages(); } catch { setError(`Something went wrong ${action === 'delete' ? 'permanently deleting' : `${action}ing`} this image.`); } finally { setActionId(null); } }
  const dialog = pendingAction ? dialogCopy(pendingAction.action) : null;
  return <div><div className="mb-6 flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-display text-2xl font-semibold text-emerald-deep">Gallery</h2><p className="mt-1 font-body text-sm text-stone">Archive images first to keep a recoverable gallery.</p></div>{scope === 'active' && <button onClick={openCreateForm} className="rounded-full bg-gold px-5 py-2 font-utility text-sm font-medium text-emerald-deep hover:bg-emerald hover:text-ivory">+ Add Image</button>}</div><div className="mb-5 flex gap-2" aria-label="Gallery catalog view"><button onClick={() => { setScope('active'); setShowForm(false); }} className={`rounded-full px-4 py-2 font-utility text-xs ${scope === 'active' ? 'bg-emerald text-ivory' : 'border border-stone/30 text-stone'}`}>Active gallery</button><button onClick={() => { setScope('archived'); setShowForm(false); }} className={`rounded-full px-4 py-2 font-utility text-xs ${scope === 'archived' ? 'bg-emerald text-ivory' : 'border border-stone/30 text-stone'}`}>Archive</button></div>{error && <div className="mb-4 flex flex-wrap items-center justify-between gap-3 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700" role="alert"><span>{error}</span>{loadFailed && <button type="button" onClick={() => void fetchImages()} className="font-utility text-xs underline">Retry loading</button>}</div>}{showForm && <form onSubmit={handleSubmit} noValidate className="mb-8 rounded-3xl bg-white p-6 shadow-lg"><h3 className="mb-4 font-display text-lg font-semibold text-ink">{editingId ? 'Edit Image' : 'New Image'}</h3><ImageUploadField value={form.imageUrl} onChange={(url) => setForm({ ...form, imageUrl: url })} />{formErrors.imageUrl && <p className="mt-1 text-xs text-red-600" role="alert">{formErrors.imageUrl}</p>}<label htmlFor="gallery-category" className="sr-only">Gallery category</label><input id="gallery-category" value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} placeholder="Category (e.g. Plated, Events, Kitchen)" aria-invalid={Boolean(formErrors.category)} aria-describedby={formErrors.category ? 'gallery-category-error' : undefined} className="mt-4 w-full rounded-xl border border-stone/20 px-4 py-2 font-body" />{formErrors.category && <p id="gallery-category-error" className="mt-1 text-xs text-red-600">{formErrors.category}</p>}<label htmlFor="gallery-caption" className="sr-only">Gallery caption</label><input id="gallery-caption" value={form.caption} onChange={(e) => setForm({ ...form, caption: e.target.value })} placeholder="Caption (optional)" className="mt-4 w-full rounded-xl border border-stone/20 px-4 py-2 font-body" /><div className="mt-6 flex gap-3"><button type="submit" disabled={saving} className="rounded-full bg-emerald px-6 py-2 font-utility text-sm text-ivory hover:bg-emerald-deep disabled:opacity-50">{saving ? 'Saving…' : 'Save'}</button><button type="button" onClick={() => setShowForm(false)} className="rounded-full border border-stone/30 px-6 py-2 font-utility text-sm text-stone hover:bg-stone/10">Cancel</button></div></form>}{loading ? <p className="font-body text-stone">Loading…</p> : images.length === 0 ? <p className="font-body text-stone">{scope === 'archived' ? 'No archived gallery images.' : 'No images yet. Add your first one above.'}</p> : <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{images.map((image) => <div key={image._id} className="overflow-hidden rounded-2xl bg-white shadow"><img src={image.imageUrl} alt={image.caption || image.category} className="h-40 w-full object-cover" /><div className="p-4"><p className="font-utility text-xs uppercase tracking-wide text-stone">{image.category}</p>{image.caption && <p className="mt-1 font-body text-sm text-stone">{image.caption}</p>}{image.archivedAt && <p className="mt-2 font-utility text-xs text-stone">Archived {new Date(image.archivedAt).toLocaleString()}</p>}<div className="mt-3 flex flex-wrap gap-2">{scope === 'active' ? <><button onClick={() => openEditForm(image)} className="rounded-full border border-emerald px-3 py-1 font-utility text-xs text-emerald hover:bg-emerald hover:text-ivory">Edit</button><button onClick={() => setPendingAction({ image, action: 'archive' })} disabled={actionId === image._id} className="rounded-full border border-stone/40 px-3 py-1 font-utility text-xs text-stone hover:bg-stone/10">Archive</button></> : <><button onClick={() => setPendingAction({ image, action: 'restore' })} disabled={actionId === image._id} className="rounded-full border border-emerald px-3 py-1 font-utility text-xs text-emerald hover:bg-emerald hover:text-ivory">Restore</button><button onClick={() => setPendingAction({ image, action: 'delete' })} disabled={actionId === image._id} className="rounded-full border border-red-400 px-3 py-1 font-utility text-xs text-red-500 hover:bg-red-700 hover:text-white">Delete</button></>}</div></div></div>)}</div>}<ConfirmDialog open={Boolean(pendingAction)} title={dialog?.title || ''} message={dialog?.message || ''} confirmLabel={dialog?.label} busy={Boolean(actionId)} busyLabel={dialog?.busy} onCancel={() => setPendingAction(null)} onConfirm={() => void confirmAction()} /></div>;
}
